# tinyowl 生产部署环境变量模板
# 复制为 deploy/.env 并填入真实值后再部署（deploy/.env 已被 git 忽略，切勿提交）
ADMIN_USERNAME=admin
# 生产务必改为强随机密码
ADMIN_PASSWORD=change-me-strong-password
# 生产务必改为强随机密钥，例如：openssl rand -hex 32
ADMIN_TOKEN_SECRET=change-me-random-secret
PROBE_ROUNDS=5
ROUND_TIMEOUT_MS=60000
